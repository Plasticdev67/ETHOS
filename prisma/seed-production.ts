/**
 * seed-production.ts
 *
 * Previously contained 9 fabricated workers (Dave Thompson, Mike Barnes, etc.),
 * fake production tasks, and sample NCRs.
 *
 * Production workers and tasks are managed through the production module.
 * Workers should be added via the admin interface.
 *
 * This file is intentionally empty. Do not add hardcoded production data here.
 */

console.log("seed-production.ts: No action — production data is managed through the application.")
